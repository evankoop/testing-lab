
const returnTwo = () => {return 2}
const greetings = (name) => {
    return `Hello, ${name}.`
}
const add = (num1, num2) => {
    const sum = num1 + num2
    return sum
}

module.exports = {
    returnTwo,
    greetings,
    add
}

